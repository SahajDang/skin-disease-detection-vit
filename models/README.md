# Trained Model Weights

The trained model weights are not included in this repository due to GitHub file size limitations.

Best model:
- `best_vit_model.keras` (~992 MB)

To obtain the model weights, contact the repository owner or use the provided download link (if available).
